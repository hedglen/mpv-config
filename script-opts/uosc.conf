# Simple uosc configuration for ultrawide monitor
timeline_size=40
controls_size=32
controls_margin=8
top_bar=no
font_scale=1.2
# Disable volume bar overlay (keep only OSD number in top left)
volume=no
